// eslint-disable-next-line no-undef
const iconsList: Record<string, NodeJS.Require> = {
  leftArrowButton: require('./icons/leftArrowButton.svg'),
  rightArrowButton: require('./icons/rightArrowButton.svg'),
};

export default iconsList;
